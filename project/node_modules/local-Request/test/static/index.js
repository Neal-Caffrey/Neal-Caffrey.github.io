/*
 *  test module_name vs 0.0.1
 *
 */

const $ = require('local-Zepto');
const juicer = require('local-Juicer');

const module_name = require('../../src/index.js');
class Index {
	constructor() {

		this.test = new module_name({
			url: 'https://www-test.yundijie.com/api/index/v1.1/info1'
		}).ajax().then((res) => {

		}, (err, status) => {
			console.log(status);
			console.log(err);
		});
		new module_name({
			url: 'https://api7-test.huangbaoche.com/basicdata/v1.0/p/goodsServiceHotCity?channelId=1108019942&offset=0&limit=8',

		}).ajax().then((res) => {
				console.log(res);
			},
			(err) => {
				console.log(err)
			})
		$('#loading').hide();
	}
};
new Index();