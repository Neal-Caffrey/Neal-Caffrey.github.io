/*
 *  test module_name vs 0.0.1
 *
 */
"use strict";
const $ = require('local-Zepto');
const juicer = require('local-Juicer');

const module_name = require('../../src/index.js');
class Index{
	constructor() {
		this.test = module_name;

		console.log(this.test);

		console.log(this.test._extend({'a' : 1}, {'b' : 2}));

		console.log(this.test._plateForm());
		console.log(this.test._isIOS());
		alert(this.test._isWX());
		console.log(this.test._getQueryJson())
		$('#loading').hide();
	}
};
new Index();