/*
 *  test module_name vs 0.0.1
 *
 */

const $ = require('local-Zepto');
const juicer = require('local-Juicer');

const module_name = require('../../src/index.js');
class Index{
	constructor() {
		this.test = new module_name({'url' : 'https://api7-test.huangbaoche.com/basicdata/v1.0/p/goodsServiceHotCity?channelId=1108019942&offset=0&limit=6'});

		this.test.ajax().then(function(a){
			console.log(a);
		});


		this.test.ajax({
			'url' : 'https://api7-test.huangbaoche.com/basicdata/v1.0/p/goodsServiceHotCity?channelId=1108019942&offset=0&limit=8'
		}).then(function(a){
			console.log(a);
		})

		this.test.promise().then((res) => {
			console.log(res);
		});
		$('#loading').hide();
	}
};
new Index();
