'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
var hasWindow = typeof window !== 'undefined';

var getAMapPosition = exports.getAMapPosition = function getAMapPosition(pos) {
  if (!pos) {
    return pos;
  }
  if (typeof pos[0] === 'number' && typeof pos[1] === 'number') {
    return hasWindow ? new window.AMap.LngLat(pos[0], pos[1]) : null;
  }
  if ('getLng' in pos) {
    return pos;
  }
  return hasWindow ? new window.AMap.LngLat(pos.longitude, pos.latitude) : null;
};

var getAMapPixel = exports.getAMapPixel = function getAMapPixel(ofst) {
  if (!ofst) {
    return ofst;
  }
  if ('getX' in ofst) {
    return ofst;
  }
  return hasWindow ? new window.AMap.Pixel(ofst[0], ofst[1]) : null;
};

var getAMapSize = exports.getAMapSize = function getAMapSize(size) {
  if (!size) {
    return size;
  }
  if ('getWidth' in size) {
    return size;
  }
  return hasWindow ? new window.AMap.Size(size.width, size.height) : null;
};

exports.default = {
  getAMapPosition: getAMapPosition,
  getAMapPixel: getAMapPixel,
  getAMapSize: getAMapSize
};